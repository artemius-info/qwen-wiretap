{
  "name": "cq-wiretap",
  "version": "1.0.9",
  "private": true,
  "description": "HTTP/HTTPS proxy for intercepting and visualizing Claude and Qwen API traffic",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.8.1",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.4",
  "engines": {
    "node": ">=20"
  }
}
